#!/usr/bin/env bash
set -euo pipefail

if [[ -x "$HOME/.local/share/uv/tools/marimo/bin/marimo" ]]; then
  exec "$HOME/.local/share/uv/tools/marimo/bin/marimo" "$@"
fi

if command -v uvx >/dev/null 2>&1; then
  exec uvx marimo "$@"
fi

echo "marimo: no marimo install found." >&2
exit 127
