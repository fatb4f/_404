#!/usr/bin/env bash
set -euo pipefail

if [[ -x "$HOME/.local/share/uv/tools/rich-cli/bin/rich" ]]; then
  exec "$HOME/.local/share/uv/tools/rich-cli/bin/rich" "$@"
fi

if command -v uvx >/dev/null 2>&1; then
  exec uvx rich-cli "$@"
fi

echo "rich: no rich-cli install found." >&2
exit 127
