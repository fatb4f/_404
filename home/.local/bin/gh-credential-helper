#!/usr/bin/env bash
set -euo pipefail

refresh_cmd="${GH_AUTH_REFRESH_BIN:-$HOME/.local/bin/gh-auth-refresh}"

if ! gh auth status -h "${GH_HOST:-github.com}" >/dev/null 2>&1; then
  "$refresh_cmd" >/dev/null
fi

exec gh auth git-credential "$@"
