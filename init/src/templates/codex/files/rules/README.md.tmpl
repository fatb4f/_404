# Codex rules surface

This directory is intentionally projected, not shell-bootstrapped.

Contract:

```txt
CUE/domain seed -> config.toml + hooks + rules + skills
runtime hooks -> evidence and small guardrails only
shell init -> no Codex policy
```

Place future Codex rule files here when the rule policy graduates from hook-local checks into declarative Codex rules.
